# fundamento de programacion
Comando: Git init
Este comando es para inicializar un repositorio local en git
solo se ejecuta una vez en proyecto
git status comando
este comando es para ver el estado actual del git en este proyecto
git add .
este comando me permite monitorear o trackear todos los archivos que no estan siendo monitoreados
el siguiente comando solo ejecutarlo cuando los archivos esten en color verde
git comit -n

git config --global user.name "CodePalmer"

git config --global user.email "correo"
 
 git remote add origin https://github.com/CodePalmer/Repositorio-del-curso-de-fundamentos-de-programacion-DUOC-2026-0030D

git remote -v

Como cambiarse y crear una rama nueva
git checkout -b main

git push origin main